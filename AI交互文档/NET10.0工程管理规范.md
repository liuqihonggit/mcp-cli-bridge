

# NET10.0工程管理规范

这里只写一次性要求的内容,不需要重复在上下文载入.
仅仅作为重头构造项目时候的参考.

## 0. 文件说明

### 给人看的文件:

#### README.md
是当前项目的说明,非常详细的版本,包括安装依赖,编译,运行等.


### 给AI看的文件(每次对话上下文都会载入):

#### CLAUDE.md
是一些通用的约定,可以被复用,包括 避坑/语法喜好/win环境.

#### AGENTS.md
是当前项目信息,包括如何构造的想法,架构依赖关系,例如AI喜欢编译时候先用`.sln`而不是`.slnx`,就会利用它写出来项目目录树(简要版本).

## 1. 全局化工程模板

### 1.1 Directory.Build.props

- 集中式属性管理，统一编译参数、版本号、代码分析规则
- **强制NativeAOT编译**，这是不可变原则
- 项目打包到npm，非AOT会导致安装崩溃

### 1.2 GlobalUsings.cs

- 全局命名空间统一导入
- `.cs`代码文件内**禁止**书写using引用，一律搬运到GlobalUsings.cs
- 命名空间冲突用简短别名在GlobalUsings.cs澄清

### 1.3 AOT兼容性要求

- 所有代码必须围绕NativeAOT编译构造
- JSON处理必须用类表示，禁止直接书写和解析JSON字符串
- 动态行为改为：直接写死(优先) 或 源码生成器(仅当前项目内使用)

### 1.4 nuget.config
一些通用技能,但是不想写 git子模块 也不想写入当前项目,
就可以写到 lib/ 目录下.
再用 nuget.config 配置文件,用于指定本项目下的 NuGet 包的下载源.
nuget.config 必须使用 Release 模式,不能用 Debug 模式,否则会占用锁定 obj 文件夹.